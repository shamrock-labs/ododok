name: ChewChewIOS
options:
  bundleIdPrefix: com.sungho
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    CODE_SIGN_STYLE: Automatic
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    DEVELOPMENT_TEAM: ""

targets:
  ChewChewIOS:
    type: application
    platform: iOS
    sources:
      - ChewChewIOS
    dependencies:
      - target: OdodokWidgets
        embed: true
    configFiles:
      Debug: Config/Secrets.xcconfig
      Release: Config/Secrets.xcconfig
    info:
      path: ChewChewIOS/Info.plist
      properties:
        CFBundleDisplayName: Ododok
        InsForgeAPIKey: "$(INSFORGE_API_KEY)"
        NSMotionUsageDescription: 식사 중 AirPods 움직임을 분석해 저작 리듬을 시각화합니다.
        NSMicrophoneUsageDescription: AirPods IMU 동작 신뢰성 분석을 위해 마이크 권한이 필요할 수 있어요. 음성 녹음은 하지 않습니다.
        UIBackgroundModes:
          - audio
        UIAppFonts:
          - Pretendard-Regular.otf
          - Pretendard-Medium.otf
          - Pretendard-SemiBold.otf
          - Pretendard-Bold.otf
          - Pretendard-Black.otf
        CFBundleURLTypes:
          - CFBundleURLSchemes:
              - chewchew
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UIStatusBarStyle: UIStatusBarStyleDefault
        NSSupportsLiveActivities: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.sungho.ododok
        TARGETED_DEVICE_FAMILY: "1"
        ENABLE_PREVIEWS: YES
        IPHONEOS_DEPLOYMENT_TARGET: "17.0"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor

  OdodokWidgets:
    type: app-extension
    platform: iOS
    sources:
      - OdodokWidgets
      - path: ChewChewIOS/Models/MealActivityAttributes.swift
    info:
      path: OdodokWidgets/Info.plist
      properties:
        CFBundleDisplayName: 오도독
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.sungho.ododok.OdodokWidgets
        TARGETED_DEVICE_FAMILY: "1"
        IPHONEOS_DEPLOYMENT_TARGET: "17.0"

  ChewChewIOSTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - ChewChewIOSTests
    dependencies:
      - target: ChewChewIOS
    settings:
      base:
        BUNDLE_LOADER: "$(TEST_HOST)"
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/ChewChewIOS.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/ChewChewIOS"
        GENERATE_INFOPLIST_FILE: YES
        IPHONEOS_DEPLOYMENT_TARGET: "17.0"

  ChewChewIOSUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - ChewChewIOSUITests
    dependencies:
      - target: ChewChewIOS
    settings:
      base:
        TEST_TARGET_NAME: ChewChewIOS
        GENERATE_INFOPLIST_FILE: YES
        IPHONEOS_DEPLOYMENT_TARGET: "17.0"
