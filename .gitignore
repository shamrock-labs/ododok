# Xcode
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/
build/
.build/
*.xcuserstate

# SPM
.swiftpm/
Packages/
Package.resolved

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Generated
*.hmap
Derived/
.package.resolved

# Secrets
Config/Secrets.xcconfig
# Firebase 구성 파일(API 키 포함) — Firebase 콘솔에서 받아 ChewChewIOS/ 아래에 둔다(ODO-80)
ChewChewIOS/GoogleService-Info.plist

# Tuist가 생성하는 Info.plist — 매번 Project.swift에서 다시 만들어진다
ChewChewIOS/Info.plist
OdodokWidgets/Info.plist
OdodokNotificationContent/Info.plist

# InsForge & AI agent skills
.insforge
.agent
.agents
.augment
.claude/settings.local.json
.cline
.github/copilot*
.kilocode
.omc
.qoder
.qwen
.roo
.trae
.windsurf

# 개인 MCP 설정 (레포 강제 안 함)
.mcp.json

# 작업 보고서 산출물 (레포에 넣지 않음)
observability-report.html

# 코드 서명 자료 (개인키 포함, 절대 커밋 금지)
.signing/
*.p12
*.mobileprovision

# 설계 스펙(리스크 기록 포함) 로컬 전용
docs/appstore-review-bg-audio-volume-design.md
